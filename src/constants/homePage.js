export const DISPLAY_NAME_KEY = 'displayName';
export const DEFAULT_DISPLAY_NAME = 'کاربر خوانا';

// The display name is owned by ProfileContext (localStorage cache synced
// with GET /user/me); DEFAULT_DISPLAY_NAME is the fallback for unset names.
